---
export interface Props {
    title: string;
}

const { title } = Astro.props;

// Handle BASE_URL with or without trailing slash
const baseUrl = import.meta.env.BASE_URL;
const faviconPath = baseUrl.endsWith("/")
    ? `${baseUrl}assets/icon.svg`
    : `${baseUrl}/assets/icon.svg`;
---

<!doctype html>
<html lang="en">
    <head>
        <meta charset="UTF-8" />
        <link rel="icon" type="image/svg+xml" href={faviconPath} />
        <meta name="viewport" content="width=device-width, initial-scale=1.0" />
        <meta
            name="description"
            content="Personalized & Gentle Fitness for Women in Toronto"
        />
        <title>{title}</title>
    </head>
    <body>
        <slot />
    </body>
</html>

<style is:global>
    * {
        margin: 0;
        padding: 0;
        box-sizing: border-box;
    }

    html {
        scroll-behavior: smooth;
    }

    body {
        margin: 0;
        min-height: 100vh;
        overflow-x: hidden;
    }
</style>
